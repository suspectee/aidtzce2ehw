[pytest]
testpaths = backend tests
pythonpath = .
markers =
    integration: starts a real HTTP/WebSocket server and tests the client protocol
filterwarnings =
    ignore:remove second argument of ws_handler:DeprecationWarning:websockets.legacy.server
